name: Snake Game

on:
  schedule:
    - cron: "0 */5 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - uses: Platane/snk@v3
        with:
          github_user_name: yasminalves16
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=E6A4B4&color_dots=#f8d7da,#f4b6c2,#eea5b7,#E6A4B4,#d98fa6

      - uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
